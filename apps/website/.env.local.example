# Google OAuth client ID for "Sign in with Google" (@react-oauth/google).
# Create the OAuth client in project website-auth-293680, then copy the
# Client ID here into a real .env.local (which is gitignored).
VITE_GOOGLE_CLIENT_ID=
